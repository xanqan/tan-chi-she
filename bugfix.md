# Bugfix 清单

## Bug 1：方向键快速连按可导致 180° 反向转弯

**位置**：`game.js` 第 89-93 行 `setDirection()` 函数

**描述**：当前防反向逻辑使用 `nextDirection` 判断，而非实际 `direction`。在同一逻辑帧内快速连按两个方向键（例如先按 ↑ 再按 ←），第二次按键会与上一帧的 `direction` 做比较而非与第一次按键后的 `nextDirection` 比较，从而可能绕过 180° 限制。

**复现**：蛇向右移动 → 快速依次按 ↑ 和 ← → 蛇会在下一帧向左移动，形成 180° 掉头。

**修复建议**：在 `setDirection` 中同时检查 `OPP[direction]` 和 `OPP[nextDirection]`，或改为在每次 `update()` 后才接受新的方向输入。

---

## Bug 2：从暂停恢复时 accumulator 未重置，可能导致连续多帧更新

**位置**：`game.js` 第 125-127 行（暂停逻辑）和第 178-190 行（主循环）

**描述**：暂停期间 `lastTime` 仍在更新（因为 `requestAnimationFrame` 始终运行），但 `accumulator` 不再累加。然而当从暂停恢复时，`lastTime` 已经是恢复时刻的时间戳，而 `accumulator` 保留着暂停前的值。如果暂停时间较长且 `lastTime` 与 `accumulator` 不同步，恢复后可能出现蛇瞬间跳帧的情况。更严重的是，`lastTime` 在暂停期间没有被更新，导致恢复后第一帧的 `dt` 会包含整个暂停期间的时间差（虽然被 `Math.min` 限制在 500ms，但仍相当于约 3-8 次更新），造成蛇在恢复瞬间跳多格。

**修复建议**：在恢复暂停时重置 `lastTime = 0` 或 `accumulator = 0`，确保恢复后不会出现时间跳跃。

---

## Bug 3：食物可能生成在蛇即将占据的位置（视觉闪烁）

**位置**：`game.js` 第 51-61 行 `spawnFood()` 函数

**描述**：`spawnFood()` 只排除蛇当前占据的格子，但没有考虑蛇正在移动的这一帧中蛇头即将到达的位置。在某些极端情况下，食物可能恰好生成在蛇头的下一个位置上，导致食物出现一瞬间就被蛇吃掉，玩家看不到食物出现的过程。

**影响**：较低，属于视觉体验问题，不影响游戏逻辑正确性。

---

## Bug 4：暂停状态不阻止方向输入

**位置**：`game.js` 第 89-93 行 `setDirection()` 函数

**描述**：`setDirection()` 只检查 `state === 'PLAYING'`，但暂停恢复后 `nextDirection` 会被设置为暂停期间按下的方向。更关键的是，当玩家从暂停恢复（按 P/Esc）时，`state` 从 `'PAUSED'` 变为 `'PLAYING'`，但此时 `nextDirection` 可能已经被暂停前的最后一次 `setDirection` 调用修改过了，导致恢复后蛇的方向可能出人意料。

**修复建议**：在恢复暂停时，将 `nextDirection` 重置为当前 `direction`，或在暂停状态下也允许方向输入（取决于设计意图）。

---

## Bug 5：窗口 resize 时蛇和食物位置可能超出新画布边界

**位置**：`game.js` 第 25-31 行 `resizeCanvas()` 和整体逻辑

**描述**：`resizeCanvas()` 只调整画布像素大小，不改变逻辑网格大小（固定 20×20）。虽然网格大小不变所以蛇和食物的坐标仍然有效，但如果未来改成动态网格大小，就会出问题。当前设计下此 bug 不会触发，但属于潜在隐患。

---

## Bug 6：游戏结束时按空格从 IDLE 状态无法察觉地不重置最高分显示

**位置**：`game.js` 第 101-104 行

**描述**：从 `GAMEOVER` 状态按空格会调用 `startGame()` → `init()`，`init()` 中调用 `updateHUD()` 会读取并显示最高分，这部分逻辑正确。但 `IDLE` 状态下按空格同样调用 `startGame()`，行为一致。此处不算 bug，但 `IDLE` 和 `GAMEOVER` 共用同一个启动逻辑，若将来需要区分两种启动行为（比如 GAMEOVER 后重开需要额外确认），则需要重构。

---

## Bug 7（重要）：self-collision 检查范围可优化，但当前实现正确

**位置**：`game.js` 第 170-175 行

**描述**：自碰撞检测遍历整个蛇身 `for (let i = 0; i < snake.length; i++)`，但实际上蛇头不可能碰到自己（`i = 0`），可以跳过第 0 项。虽然不影响正确性，但属于微优化项。

---

## 优先级排序

| 优先级 | Bug | 影响 |
|--------|-----|------|
| P0 | Bug 1：方向键 180° 反向 | 可作弊绕过核心规则 |
| P1 | Bug 2：暂停恢复时间跳跃 | 恢复后蛇瞬移多格 |
| P2 | Bug 4：暂停时方向状态 | 恢复后方向可能不符合预期 |
| P3 | Bug 3：食物闪烁 | 轻微视觉体验问题 |
| P3 | Bug 7：自碰撞遍历优化 | 性能微优化 |
| P4 | Bug 5：resize 边界 | 当前不会触发 |
| P4 | Bug 6：状态共用 | 当前无影响 |