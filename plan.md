# 贪吃蛇 HTML 小游戏 — 需求与技术方案

## 一、功能需求

### 核心玩法
- 蛇在画布内移动，吃食物后身体变长、得分增加
- 蛇撞到墙壁或自身则游戏结束
- 方向键 / WASD 控制蛇的移动方向
- 不允许 180° 反向转弯（例如向右行驶时不能立即向左）

### 游戏状态
- **开始界面**：标题 + "按空格开始" 提示
- **游戏中**：蛇移动、计分、实时渲染
- **暂停**：按 P 或 Esc 暂停/继续
- **游戏结束**：显示最终得分 + "按空格重新开始"

### 计分与难度
- 每吃一个食物 +10 分
- 每得 50 分，移动速度提升一档（最高 5 档）
- 显示当前分数与速度等级

### 额外功能
- 最高分记录（localStorage 持久化）
- 食物随机生成，不能出现在蛇身上
- 移动端触摸滑动支持

## 二、非功能需求

| 项目 | 要求 |
|------|------|
| 兼容性 | Chrome / Edge / Firefox / Safari 最新版 |
| 性能 | 60 FPS 渲染，无明显卡顿 |
| 响应式 | 画布尺寸自适应窗口，最小 320×320 |
| 无依赖 | 纯 HTML + CSS + JS，无需构建工具或第三方库 |

## 三、技术方案

### 文件结构

```
snake-game/
├── index.html    # 页面结构与样式
└── game.js       # 游戏逻辑（单文件，无需拆分）
```

### 抸心技术选型

| 模块 | 方案 | 理由 |
|------|------|------|
| 渲染 | Canvas 2D | 比 DOM 操作性能好，适合逐帧重绘 |
| 游戏循环 | `requestAnimationFrame` + 时间累加器 | 平滑渲染 + 固定逻辑帧率 |
| 状态管理 | 简单状态机（枚举 + switch） | 状态少，无需复杂框架 |
| 样式 | 内联 CSS（写在 `<style>` 中） | 单文件部署，零依赖 |
| 数据持久化 | `localStorage` | 浏览器原生 API，存最高分足够 |

### 数据模型

```js
// 蛇：坐标队列，头在 queue[0]
snake = [{x, y}, {x, y}, ...]

// 食物：单个坐标
food = {x, y}

// 方向：0=上 1=右 2=下 3=左
direction = 1

// 游戏状态
state = 'IDLE' | 'PLAYING' | 'PAUSED' | 'GAMEOVER'
```

### 网格与坐标

- 画布逻辑尺寸：20×20 格（可配置 `GRID_SIZE`）
- 每格像素 = `canvas.width / GRID_SIZE`
- 蛇和食物的坐标均为网格坐标（整数），渲染时乘以格像素转换

### 游戏循环逻辑

```
每一帧:
  累加 deltaTime
  若 累加时间 >= 移动间隔:
    清零累加器
    1. 根据方向计算新蛇头位置
    2. 检测碰撞（墙壁 / 自身）→ 游戏结束
    3. 若蛇头 == 食物 → 得分 +1，生成新食物，蛇增长
       否则 → 移除蛇尾
    4. 插入新蛇头
  渲染:
    清空画布 → 画网格背景 → 画蛇 → 画食物 → 画 UI 文字
```

### 输入处理

- 键盘：方向键 / WASD 控制方向，空格开始/重开，P/Esc 暂停
- 触摸：记录 touchstart 坐标，touchend 时计算滑动方向，转为对应方向指令
- 防抖：同一逻辑帧内仅接受一次方向变更，避免快速连按导致 180° 转弯

### 视觉设计

- 画布背景：深色（#1a1a2e）
- 蛇身：渐变绿色（头 #4ecca3 → 尾 #2d6a4f），圆角方块
- 食物：红色圆形（#e63946），带微弱脉冲动画
- 文字：白色无衬线字体，居中显示
- 网格线：极淡的线（rgba 白 0.05），增强空间感

## 四、实现步骤

1. **搭建骨架** — HTML 页面 + Canvas 居中布局 + 基础 CSS
2. **游戏循环** — requestAnimationFrame + 时间累加器框架
3. **蛇移动** — 方向输入 + 位置更新 + 碰撞检测
4. **食物生成** — 随机坐标 + 不与蛇重叠
5. **计分与难度** — 分数显示 + 速度递增
6. **状态管理** — 开始 / 暂停 / 结束界面
7. **最高分** — localStorage 读写
8. **触摸支持** — 滑动手势识别
9. **视觉打磨** — 蛇身渐变、食物动画、网格背景
10. **测试与微调** — 响应式、边界情况、性能

## 五、验收标准

- [ ] 蛇能流畅移动，方向控制响应灵敏
- [ ] 吃食物后身体增长、分数增加
- [ ] 撞墙或撞自己正确触发游戏结束
- [ ] 暂停/继续功能正常
- [ ] 最高分刷新页面后仍保留
- [ ] 移动端触摸可玩
- [ ] 窗口缩放后画布自适应